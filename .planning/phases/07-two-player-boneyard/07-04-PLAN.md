---
phase: 07-two-player-boneyard
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - client/src/components/game/GameTable.tsx
  - client/src/components/lobby/RoomLobby.tsx
  - client/src/components/game/ScorePanel.tsx
  - client/src/components/game/ScoreHistoryPanel.tsx
  - client/src/components/game/RoundEndModal.tsx
  - client/src/components/game/GameEndModal.tsx
autonomous: false
requirements: [TWO-06]

must_haves:
  truths:
    - "In 2-player mode, GameTable shows only bottom (me) and top (opponent) -- no left/right seats"
    - "RoomLobby allows starting with 2 or 4 players; shows dynamic player count"
    - "ScorePanel shows individual player names instead of Nosotros/Ellos in 2-player"
    - "RoundEndModal and GameEndModal show player names instead of team labels in 2-player"
    - "ScoreHistoryPanel shows player names instead of Nosotros/Ellos in 2-player"
    - "GameEndModal rematch counter shows X/2 in 2-player mode"
    - "Boneyard count indicator shown on game board when boneyardCount > 0"
    - "RoomLobby shows 2-seat grid (no team legend) in 2-player lobby"
  artifacts:
    - path: "client/src/components/game/GameTable.tsx"
      provides: "Conditional 2-seat vs 4-seat layout, boneyard count display"
    - path: "client/src/components/lobby/RoomLobby.tsx"
      provides: "Dynamic canStart (2 or 4), updated seat grid and waiting message"
    - path: "client/src/components/game/ScorePanel.tsx"
      provides: "Individual player name labels for 2-player"
    - path: "client/src/components/game/GameEndModal.tsx"
      provides: "Dynamic rematch vote count, individual labels"
  key_links:
    - from: "client/src/components/game/GameTable.tsx"
      to: "client/src/hooks/usePlayerPositions.ts"
      via: "passes playerCount to getPosition"
      pattern: "getPosition.*playerCount"
    - from: "client/src/components/game/GameTable.tsx"
      to: "client/src/store/gameStore.ts"
      via: "reads boneyardCount from gameState"
      pattern: "boneyardCount"
---

<objective>
Adapt all client UI components for 2-player mode: layout, lobby, score labels, modals, and boneyard display.

Purpose: Complete the user-facing experience. Players see a 2-seat layout, individual scoring labels, dynamic lobby UI, and a boneyard counter. This is the final plan that makes 2-player mode visually correct.

Output: All UI components correctly render for both 2-player and 4-player modes. Human verification confirms the full flow.
</objective>

<execution_context>
@/Users/yamirx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yamirx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-two-player-boneyard/07-CONTEXT.md
@.planning/phases/07-two-player-boneyard/07-RESEARCH.md
@.planning/phases/07-two-player-boneyard/07-01-SUMMARY.md
@.planning/phases/07-two-player-boneyard/07-02-SUMMARY.md
@.planning/phases/07-two-player-boneyard/07-03-SUMMARY.md

<interfaces>
<!-- After Plans 01-03 -->

From client/src/types/game.ts (after Plan 03):
```typescript
export interface ClientGameState {
  // ...existing...
  boneyardCount: number
  playerCount: number
}

export interface BoneyardDrawPayload {
  tile: Tile | null
  boneyardRemaining: number
  playerIndex: number
}
```

From client/src/hooks/usePlayerPositions.ts (after Plan 03):
```typescript
export function usePlayerPositions(myIndex: number, playerCount?: number): Map<number, Position>
export function getPosition(playerIndex: number, myIndex: number, playerCount?: number): Position
```

From client/src/components/game/GameTable.tsx (current structure):
```typescript
// Uses: getPosition(idx, myPlayerIndex) -- needs playerCount arg
// Computes: topIndex = (myPlayerIndex + 2) % 4 -- needs branching
// Renders: leftPlayer, rightPlayer seats -- hide in 2-player
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GameTable layout and RoomLobby for 2-player mode</name>
  <files>client/src/components/game/GameTable.tsx, client/src/components/lobby/RoomLobby.tsx</files>
  <action>
**GameTable.tsx:**

1. Extract `playerCount` from gameState: `const playerCount = gameState.playerCount ?? 4`
2. Extract `boneyardCount` from gameState: `const boneyardCount = gameState.boneyardCount ?? 0`
3. Compute `is2Player = playerCount === 2`

4. Fix index calculations:
   - `topIndex`: `is2Player ? (myPlayerIndex + 1) % 2 : (myPlayerIndex + 2) % 4`
   - `leftIndex` and `rightIndex`: only compute when `!is2Player`; set to `-1` or skip when 2-player

5. Conditionally render left/right seat sections:
   - Wrap the left opponent `<div>` and right opponent `<div>` in `{!is2Player && (...)}` guards
   - When `is2Player`, the grid still uses 3 columns but the left/right cells are empty `<div />`

6. Pass `playerCount` to `getPosition()` calls: `getPosition(topIndex, myPlayerIndex, playerCount)`

7. Update `teamInfo()` function to accept playerCount:
   - When `is2Player`: return `{ teamLabel: players[playerIndex]?.name ?? '', teamColor: playerIndex === myPlayerIndex ? '#22C55E' : '#F97316' }` -- use player names instead of "Equipo A/B"
   - When 4-player: keep existing team logic

8. Add boneyard count indicator near the board center:
   ```tsx
   {boneyardCount > 0 && (
     <div className="absolute top-2 right-2 z-10 bg-black/60 backdrop-blur-sm rounded-lg px-2.5 py-1 border border-white/10">
       <span className="font-body text-white/60 text-xs">{boneyardCount} fichas</span>
     </div>
   )}
   ```
   Place this inside the board center div (the `relative overflow-hidden w-full h-full` div), alongside the GameBoard and TurnIndicator.

**RoomLobby.tsx:**

1. Change `canStart` from `playerCount === 4` to `playerCount === 2 || playerCount === 4` (where playerCount is connected players count)
2. Determine `is2PlayerLobby = playerCount <= 2` for display purposes (show 2 seats or 4 seats)
3. Update the seat grid: change `{[0, 1, 2, 3].map(seatIndex => ...)}` to dynamically show seats based on how many players are in the room. Actually, keep showing 4 seats always -- players can join up to 4. The game start button is what validates 2 or 4.
4. Update waiting message: `Esperando {targetCount - playerCount} jugador{...}` where `targetCount` is either the next valid count (2 if < 2, 4 if 2 < count < 4)
5. Update button text: `${playerCount}/4 jugadores` -> show dynamic denominator. Since both 2 and 4 are valid, show: `canStart ? '¡Iniciar Partida!' : '${playerCount} jugadores'`
6. Keep the teams legend but conditionally hide it when there are only 2 players connected: `{playerCount > 2 && (team legend div)}`. Actually, the team legend is confusing for 2-player. Show a different message: when `playerCount === 2`, show "Modo 2 jugadores (individual)" instead of team legend.
7. Update the error case: if 3 players are connected, show "Se necesitan 2 o 4 jugadores para iniciar"
  </action>
  <verify>
    <automated>cd /Users/yamirx/Claude_Code/dominos_pr/client && npx tsc --noEmit</automated>
  </verify>
  <done>GameTable renders 2-seat layout in 2-player mode (no left/right). Boneyard count shown when > 0. RoomLobby allows starting with 2 or 4 connected players. Client compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update ScorePanel, ScoreHistoryPanel, RoundEndModal, and GameEndModal labels for 2-player</name>
  <files>client/src/components/game/ScorePanel.tsx, client/src/components/game/ScoreHistoryPanel.tsx, client/src/components/game/RoundEndModal.tsx, client/src/components/game/GameEndModal.tsx</files>
  <action>
All four components need the same pattern: detect 2-player mode from `players.length` and switch from team labels ("Nosotros/Ellos", "Equipo A/B") to individual player names.

**ScorePanel.tsx:**
- Detect `is2Player = players.length === 2`
- When `is2Player`:
  - `teamALabel` = `players[0]?.name ?? 'J1'` (no "Nosotros/Ellos")
  - `teamBLabel` = `players[1]?.name ?? 'J2'`
  - Remove " & " partner names display (just show single name per side)
- When 4-player: keep existing "Nosotros/Ellos" + partner names logic

**ScoreHistoryPanel.tsx:**
- Accept `playerCount` as prop (or derive from existing props -- add `players?: ClientPlayer[]` prop if needed, or pass `playerCount` from GameTable)
- Simplest approach: add `playerCount?: number` prop. GameTable passes `gameState.playerCount`.
- When `playerCount === 2`: `winLabel` uses player name from entry data (if available) or just "Ganador" instead of "Nosotros/Ellos". Actually, the entry `data.winningTeam` is 0 or 1, and we can map team0 -> player 0 name, team1 -> player 1 name. But ScoreHistoryPanel doesn't have player names. Pass `playerNames?: string[]` prop from GameTable: `playerNames={players.map(p => p.name)}`.
- When 2-player with playerNames: `winLabel = entry.data.winningTeam === null ? 'Trancado' : playerNames[entry.data.winningTeam] ?? 'Ganador'`
- When 4-player: keep existing "Nosotros/Ellos/Trancado" logic

**RoundEndModal.tsx:**
- Access `gameState.playerCount` (or derive from `gameState.players.length`)
- When 2-player:
  - Scores summary section: Show player names instead of "Equipo A" / "Equipo B"
  - Header: "¡Ganaste esta mano!" / "Perdiste esta mano" (singular, not "Ganamos/Perdimos")
  - Get player names from roundEndData.remainingTiles or gameState.players
- When 4-player: keep existing team labels

**GameEndModal.tsx:**
- Access playerCount from gameStore (add `gameState?.playerCount ?? 4`)
- When 2-player:
  - `winnerText`: "¡Ganaste el partido!" / "Perdiste el partido" (singular)
  - `team0Label` / `team1Label`: Use player names from `rematchPlayerNames` or gameState
  - Rematch counter: `{rematchVotes.length}/{playerCount} listos` instead of hardcoded `/4`
  - `allVoted`: `rematchVotes.length === playerCount` instead of `=== 4`
- When 4-player: keep existing logic

For all components, the pattern is:
```typescript
const is2Player = players.length === 2  // or playerCount === 2
```
Then branch labels and text accordingly.
  </action>
  <verify>
    <automated>cd /Users/yamirx/Claude_Code/dominos_pr/client && npx tsc --noEmit</automated>
  </verify>
  <done>All score/modal components show individual player names in 2-player mode. GameEndModal uses dynamic vote count. RoundEndModal uses singular pronouns. Client compiles clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of 2-player mode</name>
  <action>Verify the complete 2-player flow visually and functionally.</action>
  <what-built>Complete 2-player mode with boneyard. Server supports 2-player games with boneyard draws. Client shows 2-seat layout, individual scoring, boneyard count, and adapted labels. All existing 4-player functionality preserved.</what-built>
  <how-to-verify>
    1. Run `npm run dev` from the project root
    2. Open two browser tabs to http://localhost:5173
    3. In Tab 1: Create a room (any game mode)
    4. In Tab 2: Join the room with the code
    5. Verify lobby shows 2 connected players and the "Iniciar Partida" button is enabled
    6. Start the game from Tab 1 (host)
    7. Verify: Each player sees 7 tiles. A boneyard counter appears (should show "14 fichas" initially)
    8. Verify: Only 2 seats visible (bottom = you, top = opponent). No left/right seats
    9. Play tiles back and forth. When a player has no valid plays, verify boneyard draws happen automatically (hand grows, boneyard count decrements)
    10. Verify: Score panel shows individual player names, not "Nosotros/Ellos"
    11. Complete a hand (empty hand or blocked). Verify round-end modal shows player names, not team names
    12. Play until game ends. Verify game-end modal shows player names and rematch counter shows "X/2 listos"
    13. Test rematch with both players voting
    14. ALSO TEST 4-PLAYER: Open 4 tabs, create a 4-player game, verify everything works as before (team labels, 4 seats, no boneyard)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
`cd /Users/yamirx/Claude_Code/dominos_pr/client && npx tsc --noEmit && cd ../server && npx tsc --noEmit` -- zero errors in both workspaces.
Human verification of full 2-player game flow and 4-player regression.
</verification>

<success_criteria>
- 2-player game playable end-to-end: lobby -> game -> round end -> game end -> rematch
- Boneyard draws happen automatically when player has no valid plays
- Boneyard count visible during game
- All labels show player names (not team names) in 2-player mode
- 4-player mode unchanged (regression safe)
- Both client and server compile with zero TypeScript errors
- Human has verified the visual experience
</success_criteria>

<output>
After completion, create `.planning/phases/07-two-player-boneyard/07-04-SUMMARY.md`
</output>
