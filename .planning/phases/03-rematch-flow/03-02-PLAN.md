---
phase: 03-rematch-flow
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - client/src/components/game/GameEndModal.tsx
autonomous: false
requirements: [REM-01, REM-03, REM-06]

must_haves:
  truths:
    - "Every player sees a Revancha button in the game-end modal (not just host)"
    - "Every player sees a live X/4 listos counter with per-player vote status"
    - "When a player disconnects during voting, a toast appears and players navigate to lobby"
    - "When 4/4 vote, a celebration text appears briefly before new game starts"
  artifacts:
    - path: "client/src/components/game/GameEndModal.tsx"
      provides: "Revancha button, vote list, celebration, disconnect handling"
      contains: "Revancha"
      min_lines: 80
  key_links:
    - from: "client/src/components/game/GameEndModal.tsx"
      to: "client/src/store/uiStore.ts"
      via: "useUIStore rematchVotes, rematchCancelled"
      pattern: "rematchVotes"
    - from: "client/src/components/game/GameEndModal.tsx"
      to: "client/src/socket.ts"
      via: "socket.emit game:rematch_vote"
      pattern: "game:rematch_vote"
---

<objective>
Rewrite GameEndModal to replace host-only "Jugar de Nuevo" with the full rematch voting UX: Revancha button for all players, live vote counter, per-player status list, celebration on consensus, and disconnect cancellation toast.

Purpose: Give all 4 players equal agency to trigger a rematch and see real-time consensus progress.
Output: Fully interactive GameEndModal with rematch flow.
</objective>

<execution_context>
@/Users/yamirx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yamirx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-rematch-flow/03-CONTEXT.md
@.planning/phases/03-rematch-flow/03-01-SUMMARY.md

@client/src/components/game/GameEndModal.tsx
@client/src/store/uiStore.ts
@client/src/store/roomStore.ts
@client/src/store/gameStore.ts
@client/src/socket.ts
@client/src/components/game/PasoToast.tsx

<interfaces>
<!-- From Plan 01 — uiStore additions -->
```typescript
// Added to UIStore by Plan 01:
rematchVotes: number[]           // playerIndex array of who voted
rematchPlayerNames: string[]     // all 4 names in seat order
rematchCancelled: { playerIndex: number; playerName: string } | null
setRematchVotes: (votes: number[], playerNames: string[]) => void
setRematchCancelled: (data: { playerIndex: number; playerName: string } | null) => void
clearRematchState: () => void
```

<!-- From Plan 01 — types -->
```typescript
// client/src/types/game.ts
export interface RematchVoteUpdate { votes: number[]; playerNames: string[] }
export interface RematchCancelled { disconnectedPlayerIndex: number; disconnectedPlayerName: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite GameEndModal with rematch voting UX</name>
  <files>client/src/components/game/GameEndModal.tsx</files>
  <action>
Rewrite GameEndModal.tsx. Keep the existing header (emoji, title, scores display) but replace the bottom section entirely. Per user decisions in CONTEXT.md:

**Team labels:** Change "Equipo A" / "Equipo B" to "Nosotros" / "Ellos" (relative to myPlayerIndex team). The winning team text should say "Nosotros ganamos el partido" or "Ellos ganaron el partido" accordingly.

**Remove host-only logic:** Delete the `isHost` check and the old "Jugar de Nuevo" / "Esperando al host..." section.

**Revancha button section (replaces old Play Again):**
- Show after a 2-3 second delay (use useState + useEffect with setTimeout, `showRevancha` boolean)
- Button text: "Revancha" with green gradient background (same style as old button)
- On click: `socket.emit('game:rematch_vote', { roomCode })` and disable button
- After clicking: button shows "✓ Listo" in disabled state
- Track local vote via: `rematchVotes.includes(myPlayerIndex ?? -1)`

**Vote counter and player list (below button):**
- Only show once rematchVotes.length > 0 (someone has voted)
- Header text: `{rematchVotes.length}/4 listos` in white/60 text
- Below: list all 4 players from `rematchPlayerNames` array
  - Each row: player name (white/70 text) + status indicator
  - If playerIndex in rematchVotes: green "✓" with a CSS scale-in animation (transition from scale-0 to scale-100)
  - If not voted: dimmed "..." text (white/30)
- No team colors on player names — all neutral white/gray per user decision

**Celebration state:**
- When rematchVotes.length === 4: show "¡Revancha! 🔥" text with scale-in animation (CSS transform scale 0->1 over 300ms) replacing the vote list
- The server handles the 2s delay before game:started, so just show the celebration text

**Disconnect cancellation:**
- When `rematchCancelled` is not null: show a toast-style notification at bottom of modal
- Text: `"{playerName} se desconecto. Revancha cancelada."` in a semi-transparent red/orange bar
- Show the disconnected player with "✕" in the vote list briefly
- Navigation to lobby is handled by useSocket (Plan 01), so the modal just needs to display the cancellation state

**Animation CSS:** Add these Tailwind-compatible inline styles or utility classes:
- Scale-in for checkmarks: `transition-transform duration-200` with conditional `scale-0` / `scale-100`
- Scale-in for celebration text: same pattern with `duration-300`
- Fade-in for revancha button: `transition-opacity duration-500` with delayed appearance

**Keep existing:** The gradient border wrapper, header emoji/title, score display section. Only modify team label text and replace the bottom action section.
  </action>
  <verify>
    <automated>cd /Users/yamirx/Claude_Code/development/dominos_pr && cd client && npx tsc --noEmit</automated>
  </verify>
  <done>GameEndModal shows Revancha button for all players, live X/4 counter with per-player checkmarks, celebration on 4/4, disconnect notification. Team labels say Nosotros/Ellos. No host-only gating.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full rematch flow end-to-end</name>
  <files>client/src/components/game/GameEndModal.tsx</files>
  <action>Human verifies the complete rematch flow across 4 browser tabs.</action>
  <what-built>Complete rematch flow: server vote consensus, client state management, GameEndModal with Revancha button, live vote counter, celebration, and disconnect handling</what-built>
  <how-to-verify>
    1. Run `npm run dev` to start both client and server
    2. Open 4 browser tabs, create a room, join all 4 players
    3. Play a full game until game-end modal appears
    4. Verify: ALL 4 players see a "Revancha" button (not just host)
    5. Verify: Team labels show "Nosotros"/"Ellos" (not "Equipo A/B")
    6. Click Revancha on player 1 — verify button changes to disabled "✓ Listo"
    7. Verify: All 4 tabs show "1/4 listos" with player 1's checkmark
    8. Click Revancha on remaining 3 players one by one — counter updates live
    9. When 4/4: verify "¡Revancha! 🔥" celebration appears briefly
    10. Verify: New game starts with scores 0-0, same seats/teams
    11. Test disconnect: start another game, reach game-end, have 2 players vote, then close one tab
    12. Verify: remaining players see cancellation toast and navigate to lobby
  </how-to-verify>
  <verify>
    <automated>cd /Users/yamirx/Claude_Code/development/dominos_pr && cd client && npx tsc --noEmit</automated>
  </verify>
  <done>User confirms rematch flow works end-to-end: voting, counter, celebration, disconnect cancellation.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd client && npx tsc --noEmit` passes
- GameEndModal renders Revancha button for all players (no isHost check)
- Vote counter updates reactively from uiStore.rematchVotes
- Team labels use Nosotros/Ellos relative to player's team
- Celebration text appears when votes.length === 4
- Disconnect cancellation displays notification in modal
</verification>

<success_criteria>
All 4 players can vote for rematch via Revancha button, see live X/4 counter with per-player status, witness celebration on consensus, and get notified + redirected on disconnect cancellation. Game resets to 0-0 with same seats.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rematch-flow/03-02-SUMMARY.md`
</output>
