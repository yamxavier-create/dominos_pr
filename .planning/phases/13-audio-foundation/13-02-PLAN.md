---
phase: 13-audio-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/vite.config.ts
  - client/public/audio/test-click.mp3
autonomous: true
requirements: [AUD-03]

must_haves:
  truths:
    - "Audio MP3 files are included in the PWA precache manifest"
    - "A placeholder audio file exists in public/audio/ for Phase 14 to replace"
    - "App builds successfully with the updated workbox config"
  artifacts:
    - path: "client/vite.config.ts"
      provides: "Updated workbox globPatterns with mp3 extension"
      contains: "mp3"
    - path: "client/public/audio/test-click.mp3"
      provides: "Tiny placeholder MP3 for cache testing"
  key_links:
    - from: "client/vite.config.ts"
      to: "client/public/audio/"
      via: "workbox globPatterns includes mp3"
      pattern: "globPatterns.*mp3"
---

<objective>
Add MP3 to PWA workbox precache patterns and create a placeholder audio file so audio assets are cached for offline playback.

Purpose: Ensures audio files are available when the app is launched offline from the home screen. Phase 14 will add real SFX files to the same directory.

Output: Updated vite.config.ts with mp3 in globPatterns, placeholder MP3 in public/audio/.
</objective>

<execution_context>
@/Users/yamirx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yamirx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-audio-foundation/13-RESEARCH.md

<interfaces>
From client/vite.config.ts (line 30):
```typescript
globPatterns: ['**/*.{js,css,html,ico,png,svg,woff2}'],
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update workbox globPatterns and add placeholder audio file</name>
  <files>client/vite.config.ts, client/public/audio/test-click.mp3</files>
  <action>
**vite.config.ts** -- Single line change on line 30:
- Change `globPatterns: ['**/*.{js,css,html,ico,png,svg,woff2}']` to `globPatterns: ['**/*.{js,css,html,ico,png,svg,woff2,mp3}']`
- No other changes to the file.

**Placeholder audio file:**
- Create `client/public/audio/` directory
- Generate a tiny valid MP3 file at `client/public/audio/test-click.mp3`. This can be created programmatically using a minimal MP3 frame (use `printf` or `python3` to write the bytes). The file just needs to be a valid MP3 that workbox will include in the precache manifest. It should be under 5KB.
- Alternative: use `ffmpeg` if available to generate a 0.1s silent MP3: `ffmpeg -f lavfi -i anullsrc=r=44100:cl=mono -t 0.1 -b:a 32k client/public/audio/test-click.mp3`
- If neither approach works, download a tiny public domain click sound.
- The file is a placeholder -- Phase 14 will replace it with real SFX files.

**Verify build works:**
- Run `npm run build` from the project root to confirm vite builds successfully with the updated config and the audio file is included in the output.
  </action>
  <verify>
    <automated>cd /Users/yamirx/Claude_Code/dominos_pr && npm run build</automated>
  </verify>
  <done>vite.config.ts globPatterns includes mp3, client/public/audio/test-click.mp3 exists, project builds successfully</done>
</task>

</tasks>

<verification>
1. `npm run build` -- succeeds without errors
2. `grep mp3 client/vite.config.ts` -- confirms mp3 in globPatterns
3. `ls client/public/audio/test-click.mp3` -- file exists
4. Build output in `client/dist/` includes the audio file (check `ls client/dist/audio/`)
</verification>

<success_criteria>
- workbox globPatterns includes mp3 extension
- Placeholder MP3 file exists in client/public/audio/
- Full production build completes successfully
- Audio file appears in build output directory
</success_criteria>

<output>
After completion, create `.planning/phases/13-audio-foundation/13-02-SUMMARY.md`
</output>
