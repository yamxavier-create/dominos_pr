---
phase: 06-chat-bug-fixes-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-in-game-chat/04-VERIFICATION.md
  - .planning/phases/06-chat-bug-fixes-verification/06-VERIFICATION.md
autonomous: true
requirements: [CHAT-03, CHAT-04, CHAT-05]

must_haves:
  truths:
    - "Phase 04 VERIFICATION.md uses requirement IDs matching REQUIREMENTS.md verbatim (CHAT-03 = ReactionPicker, CHAT-04 = unread badge, CHAT-05 = reconnect history)"
    - "chatHistory reset is confirmed at both game:next_game (line ~541) and rematch consensus (line ~599) in gameHandlers.ts"
    - "CHAT-03 deviation (21 emoji vs 6 phrase spec) is documented as an accepted Phase 04 decision, not a gap"
    - "Phase 06 has its own VERIFICATION.md confirming all three phase requirements satisfied"
  artifacts:
    - path: ".planning/phases/04-in-game-chat/04-VERIFICATION.md"
      provides: "Corrected Phase 04 verification with proper CHAT-01..CHAT-07 ID mapping"
    - path: ".planning/phases/06-chat-bug-fixes-verification/06-VERIFICATION.md"
      provides: "Phase 06 verification confirming CHAT-03, CHAT-04, CHAT-05 satisfied"
  key_links:
    - from: "04-VERIFICATION.md requirements table"
      to: ".planning/REQUIREMENTS.md"
      via: "ID match: CHAT-03 = ReactionPicker, CHAT-04 = unread badge"
      pattern: "CHAT-03.*Reaction|CHAT-04.*badge"
---

<objective>
Replace the Phase 04 VERIFICATION.md (which has reversed requirement IDs) with a corrected version that maps CHAT-01 through CHAT-07 to their definitions in REQUIREMENTS.md. Then produce Phase 06's own VERIFICATION.md confirming all three gap-closure requirements are satisfied.

Purpose: The existing 04-VERIFICATION.md labels CHAT-03 as "unread badge" and CHAT-04 as "chat panel UI" — the reverse of REQUIREMENTS.md. This phase closes the audit by writing a corrected document and formally verifying Phase 06 is complete.

Output:
- `.planning/phases/04-in-game-chat/04-VERIFICATION.md` — corrected, all 7 requirements properly mapped
- `.planning/phases/06-chat-bug-fixes-verification/06-VERIFICATION.md` — phase 06 verification report
</objective>

<execution_context>
@/Users/yamirx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yamirx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-chat-bug-fixes-verification/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite 04-VERIFICATION.md with correct requirement ID mapping</name>
  <files>.planning/phases/04-in-game-chat/04-VERIFICATION.md</files>
  <action>
Overwrite `.planning/phases/04-in-game-chat/04-VERIFICATION.md` with a corrected verification report.

The existing file has requirement IDs reversed. Use REQUIREMENTS.md as the authoritative source:

- CHAT-01: Text messages broadcast to all players (up to 200 chars)
- CHAT-02: Messages appear in real time for all players
- CHAT-03: Quick reaction with one click (ReactionPicker — 21 emoji, see STATE.md decision: "Emoji-only reactions (21 total) instead of text phrases")
- CHAT-04: Unread badge shows count when panel is closed
- CHAT-05: Reconnecting player receives last 50 messages
- CHAT-06: Rate limiting — 15 messages / 10 seconds per player (REQUIREMENTS.md says "máximo 15 mensajes"; the plan-level success criteria said 5 but the authoritative spec is REQUIREMENTS.md)
- CHAT-07: Messages sanitized on server before broadcast

For each requirement write: status (SATISFIED / PARTIAL / FAILED), code evidence (file + line), and any accepted deviations.

Key facts to include (from RESEARCH.md, already code-verified):
- CHAT-03: `client/src/components/chat/ReactionPicker.tsx` exists, 21 emoji, emits `chat:send` with `type:'reaction'`. Deviation from spec (6 phrases → 21 emoji) is an accepted Phase 04 decision logged in STATE.md — document as SATISFIED with deviation note.
- CHAT-04: `client/src/components/chat/ChatButton.tsx` lines 32-35 renders red badge when `unreadCount > 0`; uiStore increments `unreadCount` only when `chatOpen === false`; `setChatOpen(true)` resets to 0. Status: SATISFIED.
- CHAT-05: `roomHandlers.ts` emits `chat:history` on rejoin. Both `game:next_game` (gameHandlers.ts ~line 541) and rematch consensus (~line 599) set `room.chatHistory = []`. Status: SATISFIED.
- CHAT-06: `chatHandlers.ts` `RATE_LIMIT_MAX = 15`. REQUIREMENTS.md says 15/10s. Status: SATISFIED. (The plan success criteria said "5" but REQUIREMENTS.md is authoritative — document this clarification.)
- CHAT-07: `chatHandlers.ts` line 34: `message.replace(/<[^>]*>/g, '').trim().slice(0, 200)`. Status: SATISFIED.
- chatHistory reset on game start: gameHandlers.ts line ~318. Status: SATISFIED.

Set frontmatter: `status: verified`, `score: 7/7 requirements satisfied`, remove gaps section (there are no real gaps once IDs are correct and REQUIREMENTS.md is used as source).

Do NOT copy the reversed ID mapping from the old file.
  </action>
  <verify>
    <automated>grep -n "CHAT-03\|CHAT-04\|CHAT-05" /Users/yamirx/Claude_Code/dominos_pr/.planning/phases/04-in-game-chat/04-VERIFICATION.md</automated>
  </verify>
  <done>04-VERIFICATION.md shows CHAT-03 mapped to ReactionPicker, CHAT-04 to unread badge, CHAT-05 to reconnect history — matching REQUIREMENTS.md. Status is "verified" with 7/7 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 06 VERIFICATION.md confirming gap closure</name>
  <files>.planning/phases/06-chat-bug-fixes-verification/06-VERIFICATION.md</files>
  <action>
Create `.planning/phases/06-chat-bug-fixes-verification/06-VERIFICATION.md`.

Phase 06 requirements are CHAT-03, CHAT-04, CHAT-05. Document that all three are satisfied:

- CHAT-03 (ReactionPicker): Code exists at `client/src/components/chat/ReactionPicker.tsx`. 21 emoji array. Each button emits `chat:send`. Spec deviation (phrases → emoji) documented as accepted Phase 04 decision in STATE.md. Gap in prior verification was an ID mapping error — no code gap exists. STATUS: SATISFIED.
- CHAT-04 (Unread badge): Code exists at `client/src/components/chat/ChatButton.tsx`. Badge renders when `unreadCount > 0`. Counter managed in `uiStore.ts` (`addChatMessage` increments when `chatOpen === false`; `setChatOpen(true)` resets). Gap in prior verification was an ID mapping error — no code gap exists. STATUS: SATISFIED.
- CHAT-05 (Reconnect history / stale history reset): Reconnect delivery via `roomHandlers.ts`. Chat history cleared on three paths: `game:start` (line ~318), `game:next_game` (line ~541), rematch consensus (line ~599). Reconnecting player never sees messages from a previous game. STATUS: SATISFIED.

Frontmatter: `phase: 06-chat-bug-fixes-verification`, `status: verified`, `score: 3/3 requirements satisfied`.

Include a "Phase Goal" section confirming the three success criteria from ROADMAP.md:
1. chatHistory reset on both next_game and rematch consensus paths — CONFIRMED
2. VERIFICATION.md exists for Phase 04 confirming CHAT-01..CHAT-07 — CONFIRMED (Task 1 above)
3. CHAT-03 and CHAT-04 formally documented with code evidence — CONFIRMED

Run TypeScript compile to confirm no code regressions:
`cd /Users/yamirx/Claude_Code/dominos_pr/client && npx tsc --noEmit && cd ../server && npx tsc --noEmit`

Include compile result in the verification report.
  </action>
  <verify>
    <automated>cd /Users/yamirx/Claude_Code/dominos_pr/client && npx tsc --noEmit && cd ../server && npx tsc --noEmit && echo "COMPILE OK"</automated>
  </verify>
  <done>06-VERIFICATION.md exists, shows 3/3 requirements satisfied (CHAT-03, CHAT-04, CHAT-05), TypeScript compile is clean.</done>
</task>

</tasks>

<verification>
1. `grep -n "CHAT-03\|CHAT-04\|CHAT-05" .planning/phases/04-in-game-chat/04-VERIFICATION.md` — IDs map correctly to ReactionPicker, unread badge, reconnect history
2. `.planning/phases/06-chat-bug-fixes-verification/06-VERIFICATION.md` exists with status: verified
3. `cd client && npx tsc --noEmit && cd ../server && npx tsc --noEmit` — clean compile (no code was changed, should be trivially green)
</verification>

<success_criteria>
- 04-VERIFICATION.md status is "verified" with 7/7 requirements correctly mapped to REQUIREMENTS.md definitions
- 06-VERIFICATION.md exists confirming CHAT-03, CHAT-04, CHAT-05 are all satisfied
- TypeScript compiles clean in both workspaces
- No code files were modified (this phase is documentation only)
</success_criteria>

<output>
After completion, create `.planning/phases/06-chat-bug-fixes-verification/06-01-SUMMARY.md`
</output>
