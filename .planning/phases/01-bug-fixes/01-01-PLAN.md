---
phase: 01-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/hooks/useSocket.ts
  - client/src/components/game/RoundEndModal.tsx
  - server/src/socket/roomHandlers.ts
autonomous: true
requirements: [BUG-01, BUG-02, BUG-03, BUG-04]

must_haves:
  truths:
    - "Tile animation targets the newest tile by sequence, not by array position"
    - "Host indicator is correct after host promotion — uses hostSocketId comparison, not seat index"
    - "Selected tile is cleared when turn passes to another player"
    - "Server starts with no dynamic require() warning; roomHandlers uses static import"
  artifacts:
    - path: "client/src/hooks/useSocket.ts"
      provides: "BUG-01 sequence-based lookup and BUG-03 selectedTile clear"
      contains: "reduce.*sequence"
    - path: "client/src/components/game/RoundEndModal.tsx"
      provides: "BUG-02 hostSocketId-based host detection"
      contains: "hostSocketId === socket.id"
    - path: "server/src/socket/roomHandlers.ts"
      provides: "BUG-04 static import of buildClientGameState"
      contains: "import { buildClientGameState }"
  key_links:
    - from: "client/src/hooks/useSocket.ts"
      to: "client/src/store/uiStore.ts"
      via: "setSelectedTile(null) call on !isMyTurn"
      pattern: "setSelectedTile\\(null\\)"
    - from: "client/src/components/game/RoundEndModal.tsx"
      to: "roomStore.hostSocketId"
      via: "hostSocketId === socket.id comparison"
      pattern: "hostSocketId.*socket\\.id"
---

<objective>
Verify that all four prerequisite bug fixes (BUG-01 through BUG-04) are correctly applied in the codebase and pass type-checking.

Purpose: These fixes were applied in commit 33780e2 and are already in the working tree. This plan validates correctness via type-checking and code inspection, confirming the phase baseline is solid before feature work begins.
Output: Clean type-check on both client and server workspaces. All four fix patterns confirmed present.
</objective>

<execution_context>
@/Users/yamirx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yamirx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bug-fixes/01-CONTEXT.md
@.planning/phases/01-bug-fixes/01-RESEARCH.md

@client/src/hooks/useSocket.ts
@client/src/components/game/RoundEndModal.tsx
@server/src/socket/roomHandlers.ts
@client/src/store/uiStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate all four bug fixes via code inspection and type-check</name>
  <files>client/src/hooks/useSocket.ts, client/src/components/game/RoundEndModal.tsx, server/src/socket/roomHandlers.ts</files>
  <action>
Verify all four fixes are correctly applied by inspecting code and running type-checks:

**BUG-01 (useSocket.ts):** Confirm the animation target uses `tiles.reduce((a, b) => a.sequence > b.sequence ? a : b)` — NOT `tiles[tiles.length - 1]`. The reduce finds the newest tile by sequence regardless of array position, which is correct for left-end plays.

**BUG-02 (RoundEndModal.tsx):** Confirm host detection uses `room?.hostSocketId === socket.id` — NOT `myPlayerIndex === 0`. This correctly handles host promotion when the original host leaves.

**BUG-03 (useSocket.ts):** Confirm `setSelectedTile(null)` is called inside the `game:state_snapshot` handler when `!gameState.isMyTurn`. This prevents ghost tile selection persisting across turn changes.

**BUG-04 (roomHandlers.ts):** Confirm `buildClientGameState` is imported via static `import` at the top of the file — NOT via `require()` at line 36. This eliminates the dynamic require warning on server startup.

Run type-checks on both workspaces to confirm no type errors:
```bash
cd client && npx tsc --noEmit
cd server && npx tsc --noEmit
```

If any fix is missing or incorrect, apply the fix per the locked decisions in CONTEXT.md.
  </action>
  <verify>
    <automated>cd /Users/yamirx/Claude_Code/development/dominos_pr/client && npx tsc --noEmit && cd /Users/yamirx/Claude_Code/development/dominos_pr/server && npx tsc --noEmit</automated>
  </verify>
  <done>All four bug fix patterns confirmed present in codebase. Both client and server type-check passes with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Validate server starts without dynamic require warning</name>
  <files>server/src/socket/roomHandlers.ts</files>
  <action>
Start the server in dev mode and confirm no dynamic require warning appears in the console output. Run for 5 seconds then kill:

```bash
timeout 5 npm run dev --workspace=server 2>&1 || true
```

Inspect output for any `require()` or `dynamic require` warnings. The server should start cleanly with only the standard "Server running on port 3001" message.

If the warning still appears, check for any other `require()` calls in `roomHandlers.ts` beyond the one at line 36 that was converted.
  </action>
  <verify>
    <automated>cd /Users/yamirx/Claude_Code/development/dominos_pr && timeout 5 npm run dev --workspace=server 2>&1 | grep -v "require" | grep -c "listening\|running\|started" || echo "Server started (check for require warnings manually)"</automated>
  </verify>
  <done>Server starts without any dynamic require warnings. The static import of buildClientGameState works correctly at runtime.</done>
</task>

</tasks>

<verification>
All four bug fixes validated:
1. BUG-01: `reduce` over `sequence` field in useSocket.ts (not array index)
2. BUG-02: `hostSocketId === socket.id` in RoundEndModal.tsx (not seat index)
3. BUG-03: `setSelectedTile(null)` on `!isMyTurn` in useSocket.ts
4. BUG-04: Static `import` of buildClientGameState in roomHandlers.ts
Both workspaces pass strict TypeScript type-checking.
</verification>

<success_criteria>
- `cd client && npx tsc --noEmit` exits 0
- `cd server && npx tsc --noEmit` exits 0
- All four fix patterns confirmed present via grep
- Server starts without dynamic require warning
</success_criteria>

<output>
After completion, create `.planning/phases/01-bug-fixes/01-01-SUMMARY.md`
</output>
