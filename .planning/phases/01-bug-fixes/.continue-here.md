---
phase: 01-bug-fixes
task: 0
total_tasks: 4
status: in_progress
last_updated: 2026-03-06T23:34:06.278Z
---

<current_state>
Phase 1 context has been gathered (01-CONTEXT.md exists and is complete). No PLAN.md has been created yet. No bug fixes have been implemented. The next step is to run /gsd:plan-phase to generate the PLAN.md, then execute it.
</current_state>

<completed_work>

- Context gathering (01-CONTEXT.md): Done — all four bugs documented with fix paths, decisions locked, deferred scope noted

</completed_work>

<remaining_work>

- Plan phase: Run /gsd:plan-phase to create PLAN.md for 01-bug-fixes
- BUG-01: Fix animation targeting wrong tile on left-end plays (`useSocket.ts:56` — use sequence-based lookup instead of array-position)
- BUG-02: Fix isHost determination in RoundEndModal (expose `isHost` boolean per-player in server room payload; client reads it instead of computing `myPlayerIndex === 0`)
- BUG-03: Fix selectedTileId not cleared on turn change (`useSocket.ts` game:state_snapshot handler — call `setSelectedTile(null)` when `!gameState.isMyTurn`)
- BUG-04: Fix dynamic require() in roomHandlers.ts:36 — convert to static ES import at top of file (no disconnect handler cleanup, that's deferred)

</remaining_work>

<decisions_made>

- BUG-01 fix: sequence-based lookup via `Math.max(...board.tiles.map(t => t.sequence))` — NOT array last-element
- BUG-02 fix: server computes and sends `isHost` boolean per-player (same per-player projection pattern as buildClientGameState) — NOT raw hostSocketId
- BUG-03 fix: `useSocket.ts` is the single correct place for this side effect, not individual components
- BUG-04 scope: ONLY static import conversion — disconnect handler duplication cleanup explicitly deferred to Phase 3/4
- Open blocker noted: Confirm whether isHost goes into existing `connection:room_info` payload or a new event (check roomHandlers.ts + roomStore.ts before implementing)

</decisions_made>

<blockers>

- [Phase 1]: Confirm isHost payload: does it go in the existing `connection:room_info` event or a new event? Read `server/src/socket/roomHandlers.ts` and `client/src/store/roomStore.ts` before implementing BUG-02.
- [Pre-work, deferred]: Rematch score semantics (reset vs accumulate) — not blocking Phase 1
- [Pre-work, deferred]: Chat quick-reaction preset phrases — not blocking Phase 1

</blockers>

<context>
Project is a Puerto Rican dominoes multiplayer game (React + Vite client, Express + Socket.io server, TypeScript strict, npm workspaces). This milestone adds social features (score history, rematch, chat) but Phase 1 must land first to fix four bugs that would break the later phases. GameEngine.ts is never touched. All four bugs have clear, isolated fix paths documented in 01-CONTEXT.md. The approach is surgical — minimum change per bug, no opportunistic refactoring.
</context>

<next_action>
Start with: Run /gsd:plan-phase to create PLAN.md for phase 01-bug-fixes, then execute the four bug fixes in order (BUG-01 through BUG-04). Before implementing BUG-02, read roomHandlers.ts and roomStore.ts to confirm the isHost payload strategy.
</next_action>
