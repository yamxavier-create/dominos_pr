---
phase: 05-video-audio-call-players-can-see-and-talk-to-each-other-in-real-time-via-webrtc-while-playing
plan: "05"
type: execute
wave: 4
depends_on: ["05-02", "05-03", "05-04"]
files_modified: []
autonomous: false
requirements:
  - CALL-06

must_haves:
  truths:
    - "TypeScript compiles clean across both client and server"
    - "Lobby toggles visible and interactive in the browser"
    - "Video tiles appear in seat positions during a game"
    - "Avatar fallback renders when camera is off or no stream"
    - "Own-tile mute/camera controls are visible and functional"
  artifacts:
    - path: "client/src/store/callStore.ts"
      provides: "Full call state — verified present"
    - path: "client/src/hooks/useWebRTC.ts"
      provides: "WebRTC lifecycle — verified present"
    - path: "server/src/socket/webrtcHandlers.ts"
      provides: "Signaling relay — verified present"
    - path: "client/src/components/player/VideoTile.tsx"
      provides: "Video/avatar rendering — verified present"
  key_links:
    - from: "TypeScript compiler"
      to: "All phase files"
      via: "npx tsc --noEmit"
      pattern: "0 errors"
---

<objective>
TypeScript compile verification and human visual check of the complete WebRTC feature.

Purpose: WebRTC features cannot be automatically integration-tested (require real browsers + media devices). This checkpoint verifies the TypeScript layer is sound and the human confirms visual correctness in browser.
Output: Phase declared complete.
</objective>

<execution_context>
@/Users/yamirx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yamirx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-video-audio-call-players-can-see-and-talk-to-each-other-in-real-time-via-webrtc-while-playing/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full TypeScript compile check</name>
  <files></files>
  <action>
Run TypeScript compiler across both workspaces and report results. Fix any type errors found before proceeding to the human checkpoint.

```bash
cd /Users/yamirx/Claude_Code/dominos_pr/client && npx tsc --noEmit
cd /Users/yamirx/Claude_Code/dominos_pr/server && npx tsc --noEmit
```

If there are errors, fix them in the relevant files (do not create new files unless needed). Common issues to watch for:
- MediaStream type not available in DOM lib — ensure tsconfig.json includes "dom" lib
- RTCSessionDescriptionInit not found — same lib issue
- signalHandlerRef type mismatch between useWebRTC.ts and useSocket.ts
- callStore methods missing from store interface vs implementation
  </action>
  <verify>
    <automated>cd /Users/yamirx/Claude_Code/dominos_pr/client && npx tsc --noEmit && cd ../server && npx tsc --noEmit && echo "BOTH_CLEAN"</automated>
  </verify>
  <done>Both workspaces compile with 0 errors. Output ends with BOTH_CLEAN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete WebRTC video/audio call feature:
- Lobby opt-in toggles (camera and mic icons per player slot)
- VideoTile component replacing PlayerSeat in all 4 seat positions
- Avatar fallback (initials in colored circle) when no stream
- Own-tile mute and camera-off controls
- Muted badge on other players' tiles
- Full peer connection lifecycle via useWebRTC (Perfect Negotiation)
- Signaling relay on server via webrtcHandlers.ts
  </what-built>
  <how-to-verify>
Run: npm run dev (from project root)

**Test 1 — Lobby toggles:**
1. Open http://localhost:5173 in a browser tab
2. Create a room and enter the lobby
3. Open 3 more tabs / windows and join the same room
4. In your tab, click the camera icon next to your name — it should toggle active/inactive
5. Click the mic icon — same
6. Confirm other tabs show your updated opt-in state in real time (icons reflect your choice)

**Test 2 — VideoTile in game:**
1. With 4 players in the lobby, start the game
2. Confirm each seat position shows a VideoTile (video or avatar circle) instead of just the plain name label
3. If no camera available: avatar circle with first two initials should appear in every seat
4. The tile count is still visible below the avatar/video

**Test 3 — Own-tile controls (if camera/mic available):**
1. In the game view, locate your tile (bottom seat)
2. Click the mic button — your mic should mute (no renegotiation, no page reload)
3. Click again — mic unmutes
4. Click the camera button — video should stop; avatar appears
5. Other players should see a muted badge on your tile when you mute

**Test 4 — Avatar fallback:**
1. If no camera/mic permission is granted, confirm that avatar circles with initials appear for all seats
2. Confirm game play (tile placement, turn passing) still works normally with VideoTiles present

Note: Actual video between peers may not work on localhost due to STUN (localhost-to-localhost peer connections may not need STUN and may work directly). Full test requires ngrok or LAN.
  </how-to-verify>
  <resume-signal>Type "approved" if the feature looks correct, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- TypeScript: 0 errors in both workspaces
- Lobby: camera/mic toggle icons visible and interactive
- Game: VideoTile in all 4 seat positions
- Avatar fallback: visible when no stream
- Own-tile controls: visible for bottom (local player) seat
- Game mechanics: play, pass, scoring unaffected
</verification>

<success_criteria>
Phase 5 is complete when TypeScript compiles clean AND human confirms VideoTile is visible in all seat positions with correct fallback behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/05-video-audio-call-players-can-see-and-talk-to-each-other-in-real-time-via-webrtc-while-playing/05-05-SUMMARY.md`
</output>
