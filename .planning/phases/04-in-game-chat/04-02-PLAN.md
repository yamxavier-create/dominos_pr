---
phase: 04-in-game-chat
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - client/src/components/chat/ChatPanel.tsx
  - client/src/components/chat/ChatButton.tsx
  - client/src/components/chat/ReactionPicker.tsx
  - client/src/pages/GamePage.tsx
autonomous: false
requirements: [CHAT-01, CHAT-02, CHAT-03, CHAT-04]

must_haves:
  truths:
    - "Player can open chat panel, type a message, send it, and see it appear"
    - "Player can send a quick reaction with a single tap"
    - "Unread badge shows count when panel is closed and messages arrive"
    - "Chat panel shows messages from all players with names and timestamps"
  artifacts:
    - path: "client/src/components/chat/ChatPanel.tsx"
      provides: "Message list, text input, reaction picker"
      min_lines: 50
    - path: "client/src/components/chat/ChatButton.tsx"
      provides: "Toggle button with unread badge"
      min_lines: 15
    - path: "client/src/components/chat/ReactionPicker.tsx"
      provides: "Quick reaction grid"
      min_lines: 15
  key_links:
    - from: "client/src/components/chat/ChatPanel.tsx"
      to: "client/src/store/uiStore.ts"
      via: "useUIStore for chatMessages, chatOpen"
      pattern: "useUIStore"
    - from: "client/src/components/chat/ChatPanel.tsx"
      to: "socket"
      via: "socket.emit('chat:send')"
      pattern: "chat:send"
    - from: "client/src/pages/GamePage.tsx"
      to: "client/src/components/chat/ChatButton.tsx"
      via: "import and render"
      pattern: "ChatButton"
---

<objective>
Build the chat UI: ChatPanel (message list + input), ChatButton (toggle + unread badge), and ReactionPicker (quick reactions). Wire into GamePage.

Purpose: Delivers the visible chat feature users interact with.
Output: Fully functional in-game chat UI.
</objective>

<execution_context>
@/Users/yamirx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yamirx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-in-game-chat/04-RESEARCH.md
@.planning/phases/04-in-game-chat/04-01-SUMMARY.md
@client/src/store/uiStore.ts
@client/src/pages/GamePage.tsx
@client/src/socket.ts

<interfaces>
<!-- From Plan 01 (uiStore additions) -->
```typescript
export interface ChatMessage {
  id: string
  playerIndex: number
  playerName: string
  content: string
  type: 'text' | 'reaction'
  timestamp: number
}

// uiStore actions available:
chatMessages: ChatMessage[]
chatOpen: boolean
unreadCount: number
addChatMessage: (msg: ChatMessage) => void
setChatMessages: (msgs: ChatMessage[]) => void
setChatOpen: (open: boolean) => void
clearChatState: () => void
```

<!-- Socket emit pattern (from client/src/socket.ts) -->
```typescript
import { socket } from '../socket'
socket.emit('chat:send', { message: string, type: 'text' | 'reaction' })
```

<!-- Quick reactions allowlist (must match server) -->
```typescript
const QUICK_REACTIONS = ['¡Capicú!', '¡Trancado!', '¡Buena jugada!', '¡Mala suerte!', '🔥', '🤡'] as const
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatPanel, ChatButton, and ReactionPicker components</name>
  <files>client/src/components/chat/ChatPanel.tsx, client/src/components/chat/ChatButton.tsx, client/src/components/chat/ReactionPicker.tsx</files>
  <action>
1. Create `client/src/components/chat/ReactionPicker.tsx`:
   - Define `QUICK_REACTIONS` array matching server allowlist
   - Render a row/grid of buttons, each emitting `socket.emit('chat:send', { message: reaction, type: 'reaction' })`
   - Tailwind: flex wrap, gap-2, small rounded buttons with hover states
   - Export `QUICK_REACTIONS` for potential reuse

2. Create `client/src/components/chat/ChatButton.tsx`:
   - Fixed position button (bottom-right, above any existing controls)
   - Chat bubble icon (use an inline SVG — no icon library)
   - Unread badge: `useUIStore(s => s.unreadCount)` — show red circle with count when > 0, cap display at "9+"
   - onClick: `useUIStore.getState().setChatOpen(!chatOpen)`

3. Create `client/src/components/chat/ChatPanel.tsx`:
   - Slide-in panel from right side (similar pattern to ScoreHistoryPanel overlay)
   - Position: fixed, right-0, full height, w-80 (or similar), z-50, dark semi-transparent background
   - Close button at top
   - Message list: scrollable div, auto-scroll to bottom on new messages (useEffect + ref.scrollIntoView)
   - Each message shows: playerName (colored by playerIndex — use 4 distinct colors), content, relative time
   - Reaction messages: render with larger font / emoji styling to distinguish from text
   - Text input at bottom: `<input maxLength={200}>` with send button
   - On submit: `socket.emit('chat:send', { message: inputValue.trim(), type: 'text' })`, clear input
   - `onKeyDown` with stopPropagation to prevent game key handlers from firing
   - Embed ReactionPicker above or below the text input
   - On open: focus the text input
  </action>
  <verify>
    <automated>cd /Users/yamirx/Claude_Code/development/dominos_pr/client && npx tsc --noEmit</automated>
  </verify>
  <done>All three chat components exist, type-check clean, import from uiStore and socket correctly. ChatPanel has message list, text input, reaction picker. ChatButton has unread badge. ReactionPicker emits reactions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ChatButton and ChatPanel into GamePage</name>
  <files>client/src/pages/GamePage.tsx</files>
  <action>
1. Import ChatButton and ChatPanel into GamePage.tsx
2. Render ChatButton (always visible during game)
3. Render ChatPanel conditionally based on `useUIStore(s => s.chatOpen)`
4. Ensure z-index layering: ChatPanel above game board but below modals (round-end, game-end)
  </action>
  <verify>
    <automated>cd /Users/yamirx/Claude_Code/development/dominos_pr/client && npx tsc --noEmit</automated>
  </verify>
  <done>GamePage renders ChatButton and ChatPanel. Chat is accessible during gameplay. Panel does not interfere with game modals.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete in-game chat: text messages, quick reactions, unread badge, and chat panel UI</what-built>
  <how-to-verify>
    1. Run `npm run dev` from project root
    2. Open 4 browser tabs, create a room and join with 4 players
    3. Start a game, then click the chat button (bottom-right) on any player
    4. Type a message and send — verify all 4 tabs see it
    5. Close the chat panel on one tab, send a message from another — verify unread badge appears
    6. Open panel again — verify badge clears
    7. Tap a quick reaction (e.g. "🔥") — verify it appears in chat for all players
    8. Try sending more than 5 messages rapidly in 10 seconds — verify rate limiting kicks in (messages stop being broadcast)
    9. Try sending `<script>alert('xss')</script>` — verify HTML tags are stripped
    10. Disconnect one player and reconnect (same name) — verify they see chat history
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Full build passes:
```bash
cd /Users/yamirx/Claude_Code/development/dominos_pr && npm run build
```

Manual verification of all 5 success criteria from ROADMAP Phase 4.
</verification>

<success_criteria>
- Chat panel opens/closes from game view
- Text messages broadcast to all 4 players in real time
- Quick reactions send with single tap
- Unread badge shows count when panel is closed
- Chat history preserved on reconnect
- Rate limiting and sanitization enforced
</success_criteria>

<output>
After completion, create `.planning/phases/04-in-game-chat/04-02-SUMMARY.md`
</output>
